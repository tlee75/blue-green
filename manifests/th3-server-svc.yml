apiVersion: v1
kind: Service
metadata:
  name: th3-server-svc
spec:
  selector:
    app: th3-server
    env: ENV_ENVIRONMENT
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: NodePort
